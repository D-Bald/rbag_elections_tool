<.header>
  Option {@option.id}
  <:subtitle>This is a option record from your database.</:subtitle>
  <:actions>
    <.link href={~p"/wahlen/#{@wahl_id}/fragen/#{@frage_id}/optionen/#{@option}/edit"}>
      <.button>Edit option</.button>
    </.link>
  </:actions>
</.header>

<.list>
  <:item title="Wert">{@option.wert}</:item>
</.list>

<.back navigate={~p"/wahlen/#{@wahl_id}/fragen/#{@frage_id}/optionen"}>Back to optionen</.back>
